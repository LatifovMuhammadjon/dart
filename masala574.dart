import 'dart:io';

void main() {
  List<int> nums =
      stdin.readLineSync()!.split(" ").map((e) => int.parse(e)).toList();
  List<String> arr = [];
  int n = nums[0];
  int m = nums[1];
  int s = 0;
  for (int i = 0; i < n; i++) {
    stdin.readLineSync()!;
  }
  print(4);
  exit(0);
}
